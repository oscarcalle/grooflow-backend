# Credenciales SSH Hostinger (NO subir a git).
# Copia a ssh.env en esta misma carpeta:
#   cp deploy/ssh.env.example deploy/ssh.env

SSH_HOST=
SSH_PORT=65002
SSH_USER=
SSH_PASS=

# Raíz del sitio en Hostinger (sin barra final).
SSH_REMOTE_DIR=
